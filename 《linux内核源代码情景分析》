day1
  1.
  当讨论一个CPU是多少位时，指的是处理器中"算术逻辑单元ALU"的宽度，
  系统总线中的数据线部分，称为"数据总线"通常和ALU具有相同的宽度。
  2.
  在内存管理机制有两种1.段式管理  2.页式管理
  段式管理：将指令中结合段寄存器使用的32位逻辑地址映射成同样是32位的物理地址。
  页式管理：对32位的线性地址有新的解释(10位页面目录10位页面表12位页面偏移)
  从空间效率考虑，先找到目录项，再找到页面描述项...
  3.linux内核中的C代码
  GNU的C和C++是合为一体的，
  "inline"和"const"的使用。
  inline函数的使用与#define宏定义相似，另一方面有利于程序调试，编译时不加优化，
  则这些inline函数就是普通函数，调试好后，采用优化编译再编译一次，这些inline函数
  就像宏操作一样融入引用处的代码中，有利于提高运行效率。
  为了支持64位的CPU结构，gcc增加了long long int 类型。
  inline和__inline__或者asm和__asm__是等价的。只是保留字的问题。

  关于宏定义，有些奇怪的地方。
  #define func(a,b)     do{memcpy(c,a,b); c += b;}while(0)
